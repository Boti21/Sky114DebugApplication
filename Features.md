# Amaury
- detecting ball and lugages
- seeing the processed image
- not much is needed since no real time debugging

# Theo
- similar to Amaury

# Henrik
- xy map maybe z indicated by height
- every few seconds draws current pos, heading and index instead of line bc maybe plotting doesnt support that
- see IMU readings as a graph, line series, acc gyro magnetometer
- robobot/
- robobot/IMU <timestamp> accX accY etc..
- see if it can be logged from web assembly into a log file
- csv export fil


# Alex
- add servo up and down for the manual control
- add static map to the map so that the current pos can be overlayed
- check if live pos is possible (low prio)

